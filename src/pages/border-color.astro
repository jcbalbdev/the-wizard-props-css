---
import Layout from "../layouts/Layout.astro";
import Button from "../components/Button.astro"
---

<Layout title="TheWizar[Props] | Border-color">
    <main class="p-1 my-1 mx-auto w-11/12  row-start-2 row-end-6">
      <div class="p-1  h-full grid grid-cols-2 grid-rows-5 gap-4">
          <!-- definicion de la propiedad -->
          <p class="m-0 p-4 font-normal text-center text-base text-blue-950 bg-slate-50 rounded-lg col-span-2 row-start-1 row-end-2 ">
            Define el color de los border del elemento.
          </p>
          <!-- valores de la propiedad -->
          <section class="m-0 p-4 overflow-auto bg-yellow-300  rounded-lg col-span-2 row-start-2 row-end-4 md:col-span-1 md:row-start-2 md:row-end-6" id="scr" >
            <div class="p-0 grid grid-cols-1 grid-rows-7 gap-4" >
              <Button title="border-color: transparent;" body="Aplica un color transparente a los bordes que seguiran manteniendo el espacio definido por el valor de border-width"/>
              <Button title="border-color: red;" body='Puedes usar cualquier color predefinido en css, por ejemplo: red, green, blue, etc. '/>
              <Button title="border-color: #05ffb0;" body="Puedes usar codigos hexadecimales para definir un color, por ejemplo: #202020, #ffffff, etc."/>
              <Button title="border-color: rgb(50, 115, 220);" body="Puedes usar el modelo de color rgb() que define los colores como una mezcla entre rojo(red), verde(green) y azul(blue).Cada uno de estos puede tener un valor entre 0 y 255."/>
              <Button title="border-color: rgba(50, 115, 220, 0.3);" body="Puedes usar el modelo de color rgba() que define los colores como una mezcla entre rojo(red), verde(green), azul(blue) y un valor alfa(alpha) que define el nivel de opacidad. El valor de alfa puede ir desde 0 (transparente y 1 (opaco)."/>
              <Button title="border-color: hsl(14, 100%, 53%);" body="Puedes usar el modelo de color hsl() que define los colores como parametros de tono(hue), saturacion(saturation) y luminisidad(luminosity). El tono puede tener un valor entre 0 y 359, la saturacion entre 0% y 100% y la luminisidad entre 0% y 100%."/>
              <Button title="border-color: hsla(14, 100%, 53%, 0.6);" body="Puedes usar el modelo de color hsla() que define los colores como parametros de tono(hue), saturacion(saturation), luminisidad(luminosity) y un valor alfa(alpha). El valor de alfa puede ir desde 0 (transparente y 1 (opaco)."/>

            </div>
          </section>
          <!-- interfaz de la propiedad -->
          <div class="m-0 p-0   rounded-lg col-span-2 row-start-4 row-end-6 md:col-span-1 md:row-start-2 md:row-end-6">
            <div class="w-full h-full relative ">
              <div class="box absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-40 h-40 bg-yellow-300 border-2 border-b-4 border-blue-950 rounded-2xl"><div class="w-full h-fullrelative ">
                <div class=" text-5xl font-bold text-blue-950   absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">JC</div>
              </div>
              </div>
            </div>
          </div>
      </div>
    </main>
</Layout>
<script>

  const btns = document.querySelectorAll('button.btn');
  
  btns.forEach((btn) => {
  
  btn.onclick = () => {
  let propiedad = btn.firstElementChild.innerHTML;
  document.querySelector('div.box').setAttribute('style', propiedad);
  }
  
  });
  </script>
<style>
	#scr::-webkit-scrollbar{
    width: 0.5rem;
    height: 0.5rem;
	}
  #scr::-webkit-scrollbar-thumb{
    background:rgb(23 37 84);
    border-radius: 16px;
	}
  #scr::-webkit-scrollbar-thumb:hover {
    background: white;
  }
  .container::-webkit-scrollbar-track {
    border-radius: 16px;
  }

</style>